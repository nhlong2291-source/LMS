import React from "react";

export default function SessionSettings() {
  return (
    <div style={{ padding: 16 }}>
      <h3>Session Settings</h3>
      <p>
        Configure session timeout, inactivity handling and single sign-on
        options (placeholder).
      </p>
    </div>
  );
}
